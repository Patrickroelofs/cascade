DATABASE_URL=postgres://postgres:postgres@localhost:5432/cascade
# Must be identical in apps/web (shared session cookie signing).
BETTER_AUTH_SECRET=change-me-to-a-random-string-of-32-plus-chars
# This app's own origin.
BETTER_AUTH_URL=http://localhost:3001
# Production only: set to .cascadelist.com so the session spans subdomains.
# COOKIE_DOMAIN=.cascadelist.com
# Where unauthenticated users are sent to log in (apps/web).
VITE_WEB_URL=http://localhost:3000
PORT=3001
BETTER_AUTH_GITHUB_CLIENT_ID=
BETTER_AUTH_GITHUB_CLIENT_SECRET=
BETTER_AUTH_GOOGLE_CLIENT_ID=
BETTER_AUTH_GOOGLE_CLIENT_SECRET=
# Comma-separated origins allowed for authenticated cross-origin requests.
# Defaults to localhost:3000/3001 plus cascadelist.com when unset.
# TRUSTED_ORIGINS=http://localhost:3000,http://localhost:3001
# Minimum log level (debug|info|warn|error); defaults to info in production, debug in dev.
# LOG_LEVEL=info
# Required by db:seed:prod - password for the seeded dev user in production.
# SEED_DEV_PASSWORD=
